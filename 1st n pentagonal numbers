#include<stdio.h>
int main()
{
    int n,p=0;
    scanf("%d",&n);
    if(n>0)
    {
    for(int i=1;i<=n;i++)
    {
    p=(i*((3*i)-1))/2;
   printf("%d\n",p);
}
}
else
{
    printf("Invalid Input");
}
}

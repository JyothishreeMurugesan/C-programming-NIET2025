#include<stdio.h>
int main()
{
    int i,n;
    scanf(" %d ",&n);
    if(n<0)
    {
        printf("Invalid input");
    }
    else
    {
      for(i=1;i<=n;i++)
      {
          if (n%i==0)
          {
            printf("%d ",i);
          }
      }
    }
}

#include<stdio.h>
int main()
{
    int a,b,i,sum=0;
    scanf("%d %d",&a,&b);
    if((a<0)||(b<0))
    {
        printf("Invalid input");
    }
    else
    {
        for(i=a;i<=b;i++)
        {
        if(i%7==0)
        {
            sum+=i;
        }
    }
    printf("%d",sum);
    }
}

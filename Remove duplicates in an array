#include<stdio.h>
int main()
{
    int a[10],i,j,n;
    scanf("%d",&n);
    if(n>0)
    {
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        int c=0;`
        for(j=0;j<i;j++)
        {
        if(a[i]==a[j])
        {
            c++;
            break;
        }
        }
        if(c==0)
        {
            printf("%d ",a[i]);
        }
    }
    }
    else
    {
        printf("Invalid input");
    }
    
}

#include<stdio.h>
int main()
{
    int i,temp,n,a[10];
    scanf("%d",&n);
    if(n>0)
    {
        for(i=0;i<n;i++)
        {
            scanf("%d",&a[i]);
        }
        temp=a[0];
        for(i=0;i<n;i++)
        {
            a[i]=a[i+1];
        }
        a[n-1]=temp;
    
     for(i=0;i<n;i++)
     {
         printf("%d ",a[i]);
     }
    }
     else
     {
         printf("Invalid Input");
     }
}

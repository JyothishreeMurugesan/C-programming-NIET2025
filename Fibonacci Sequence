#include<stdio.h>
int main()
{
    int n,i,a=0,b=1,c;
    scanf("%d",&n);
    if(n<=0 || n>100)
    {
        printf("Invalid input");
        return 0;
    }
else
{
    printf("%d ",a);
    printf("%d ",b);
    for(i=1;i<=n;i++)
    {
    c=a+b;
    a=b;
    b=c;
    if(c>n)
    {
        break;
    }
    printf("%d ",c);
    } 
}
}

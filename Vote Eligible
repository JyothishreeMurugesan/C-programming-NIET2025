#include<stdio.h>
int main()
{
    int age;
    scanf("%d",&age);
    if(age<0)
    {
        printf("Invalid input");
    }
    else if(age>=18)
    {
        printf("Eligible");
    }
    else if(age<18)
    {
        printf("Not eligible");
    }
}
